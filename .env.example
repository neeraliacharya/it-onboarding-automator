# Server
PORT=
NODE_ENV=

# Logging
# Valid values: debug | info | warn | error
LOG_LEVEL=

# Database
DB_PATH=

# Ollama (stretch goal — optional, skipped silently if not running)
OLLAMA_BASE_URL=
OLLAMA_MODEL=
